/**
 * Configuration de l'application
 */

// ⚠️ ACTIVEZ LE MODE DEMO POUR TESTER L'INTERFACE SANS BACKEND
export const DEMO_MODE = true;

export const config = {
  demoMode: DEMO_MODE,
};

export default config;
